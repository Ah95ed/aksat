# تقرير التدقيق الشامل (AUDIT.md)

## مطابقة تطبيق Flutter مع المرجع (Vue 3 + Tailwind) وفق البرومبت المعتمد

تاريخ التدقيق: 21 سبتمبر 2026  
بيئة التدقيق: مشروع Flutter في `D:\aksat` مقارنة بالمرجع `reference_web/` وملف `aksat_aksatmaadmaad.sql`.

---

## 1. المعمارية وهيكل المشروع (القسم 3)

| العنصر / الملف المستهدف                     |  الحالة  | الفرق والتقييم التفصيلي                                                                                                                                                                                                          |
| ------------------------------------------- | :------: | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| معمارية Clean Architecture + MVC + Provider | ⚠️ يختلف | الهيكل الأساسي موجود في `features/*/{data,domain,presentation}` ولكن بعض الملفات مكررة في الجذر (`lib/theme`, `lib/responsive`) بدلاً من `lib/core/theme` و `lib/core/responsive`. لا توجد مجلدات `core/widgets` مفعّلة (فارغة). |
| `core/config/app_config.dart`               | ⚠️ يختلف | يحمل `baseUrl` والمهلات فقط؛ ينقصه رقم الدعم الفني الموحد (`07706118992`) ورقم الواتساب (`9647706118992`) وإصدار التطبيق `v1.0.0`.                                                                                               |
| `core/network/api_client.dart`              | ✅ مطابق | يعتمد Dio مع `validateStatus: (_) => true` ويفوض المعالجة لـ `ApiResponseHandler`.                                                                                                                                               |
| `core/network/auth_interceptor.dart`        | ✅ مطابق | يضيف ترويستي `Authorization: Bearer` و `X-Authorization: Bearer` معاً من التخزين الآمن.                                                                                                                                          |
| `core/network/api_response_handler.dart`    | ⚠️ يختلف | يعالج 401/403/429/404/409 و `success != true`، ولكن نصوص رسائل أخطاء الشبكة والمهلة تختلف عن النص المطلوب حرفياً: «تعذر الاتصال بالخادم، تأكد من اتصالك بالإنترنت.».                                                             |
| `core/network/api_endpoints.dart`           | ✅ مطابق | يحتوي الثوابت للمسارات بصيغة `.php`.                                                                                                                                                                                             |
| `core/storage/*` (secure_storage & prefs)   | ✅ مطابق | خدمات التخزين الآمن والتفضيلات تعمل بشكل سليم.                                                                                                                                                                                   |
| `core/theme/*`                              | ⚠️ يختلف | ملفات الثيم موجودة في `lib/theme` بدلاً من `lib/core/theme`، وتستخدم ألوان Teal (`0xFF0F766E`) بدلاً من ألوان Tailwind الزرقاء (`blue600 #2563eb`)، والظلال والتدرجات الفيزيائية غير مطابقة.                                     |
| `core/responsive/*`                         | ⚠️ يختلف | موجود في `lib/responsive`، يحتاج ربط محكم مع `AdaptiveShell` لدعم الدرج والقائمة الثابتة والتمركز عند 1280.                                                                                                                      |
| `core/l10n/*`                               | ❌ ناقص  | مجلد `core/l10n` فارغ، ولا توجد ملفات `app_ar.arb` و `app_en.arb` ولا إعدادات `gen-l10n` كاملة.                                                                                                                                  |
| `core/utils/parsers.dart`                   | ✅ مطابق | يتضمن `toNum`, `toInt`, `toId`, `toStr`, `toBool` بصورة متسامحة مع نصوص الخادم.                                                                                                                                                  |
| `core/utils/money_formatter.dart`           | ⚠️ يختلف | يستخدم `NumberFormat.currency` مع لغة النظام ودوال `compact` غير موجودة في الويب؛ المطلوب دالة `formatAmount(x)` مطابقة لقاعدة `en_US` بحد أقصى خانتين عشريتين وبلا أصفار زائدة وقواعد وضع الرمز.                                |
| `core/utils/date_formatter.dart`            | ⚠️ يختلف | ينقصه `formatDateShort` (`dd/MM/yyyy`)، `formatDateNumeric` (`yyyy/M/d` بلا أصفار)، `formatPeriod` للفترات، و`daysUntilText`.                                                                                                    |
| `core/utils/whatsapp_launcher.dart`         | ❌ ناقص  | غير موجود كملف مستقل وموحد للتعامل مع روابط واتساب والاتصال الهاتفي وقوالب الاستبدال.                                                                                                                                            |
| `core/widgets/` (الويدجتات المشتركة)        | ❌ ناقص  | المجلد فارغ تماماً؛ لم يتم استخراج الويدجتات المشتركة المعيارية.                                                                                                                                                                 |

---

## 2. رموز التصميم Design Tokens (القسم 4)

| الرمز / الخاصية                      |  الحالة  | الفرق والتقييم التفصيلي                                                                                                                                                        |
| ------------------------------------ | :------: | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| باليتة الألوان الافتراضية (Tailwind) | ⚠️ يختلف | الكود الحالي يستعمل لوحة ألوان Teal بدلاً من تدرجات `blue`, `green`, `emerald`, `amber`, `red`, `purple`, `indigo`, `gray` المحددة في الجدول 4.1.                              |
| التدرجات الفيزيائية وخلفية الصفحة    | ⚠️ يختلف | خلفية الصفحة في الويب هي تدرج 135° من `#f0f4f8` إلى `#e2e8f0` (`Alignment.topLeft` إلى `bottomRight`). الكود الحالي يستخدم لوناً مسطحاً.                                       |
| خط Cairo والأوزان                    | ⚠️ يختلف | ملف الخط `cairo-variable.ttf` مضاف محلياً، لكن لم يتم ضبط سلم النصوص الدقيق (`xs` إلى `4xl`) مع حساب `TextStyle.height = lineHeight / fontSize` و `leadingDistribution: even`. |
| الإيموجي كأيقونات                    | ⚠️ يختلف | الكود الحالي يستبدل الإيموجي بأيقونات Material (`Icons.*`) خلافاً لشرط البرومبت الصريح باستعمال نفس الإيموجي كنصوص.                                                            |
| مقاييس المسافات والأنصاف والظلال     | ⚠️ يختلف | `AppShadows` الافتراضية للـ Tailwind (`sm`, `md`, `lg`, `xl`, `2xl`) غير معرّفة بدقة كـ BoxShadows مطابقة لقيم CSS.                                                            |
| نقاط الكسر والتجاوب                  | ⚠️ يختلف | تم استخدام نقاط كسر Material (600) في بعض الأماكن بدلاً من نقاط كسر Tailwind (`768` للموبايل/التابلت و `1024` للديسكتوب).                                                      |

---

## 3. الويدجتات المشتركة Core Widgets (القسم 5)

| الويدجت                            | الحالة  | الفرق والتقييم التفصيلي                                                                                                                  |
| ---------------------------------- | :-----: | ---------------------------------------------------------------------------------------------------------------------------------------- |
| `AppButton`                        | ❌ ناقص | غير موجود؛ الصفحات تستخدم أزرار Material 3 (`FilledButton`, `TextButton`) بقياسات وألوان وظلال مختلفة.                                   |
| `AppTextField`                     | ❌ ناقص | غير موجود؛ تستخدم الحقول `InputDecoration` العادية دون مطابقة حلقة التركيز `focus:ring-2 focus:ring-blue-500` أو بطاقة التسمية المنفصلة. |
| `AppCard`                          | ❌ ناقص | تستخدم الشاشات `Card` الافتراضي بنصف قطر وزوايا مختلفة عن مواصفة الويب (`rounded-2xl` = 16، ظل `md`، حشوة 24).                           |
| `AppBadge`                         | ❌ ناقص | غير موجود؛ يتم عمل شارات يدوية متباينة أو استخدام نصوص عادية.                                                                            |
| `AppSpinner`                       | ❌ ناقص | يستخدم `CircularProgressIndicator` التقليدي بدلاً من القوس الدائري الخطي المطابق لـ `border-b-2 border-blue-600`.                        |
| `LoadingView` & `EmptyView`        | ❌ ناقص | غير موجودة كمعيار موحد بحشوات ورسائل مطابقة للويب.                                                                                       |
| `AppTable`                         | ❌ ناقص | الجداول غير مجهزة برأس مخصص، أو إمكانية تمرير أفقي موحدة، أو إخفاء الأعمدة الموسومة بـ `hideOnMobile` تلقائياً تحت 768px.                |
| `FilterPill`                       | ❌ ناقص | تستخدم الشاشات `ChoiceChip` من Flutter بألوان وارتفاعات لا تطابق أزرار الفلترة في الويب.                                                 |
| `AppModal`                         | ❌ ناقص | غير موجود؛ الحوارات تستخدم `AlertDialog` التقليدي بدلاً من لوحة المودال المحددة بأقصى عرض 448 أو 672 مع طبقة تعتيم 50%.                  |
| `InfoBanner`                       | ❌ ناقص | غير موجود كويدجت بحد يميني فيزيائي 4px وخلفيات ملونة مطابقة.                                                                             |
| `StatTile`                         | ❌ ناقص | غير موجود كويدجت صغير موحد بحشوة 12 وخلفية ملونة ورقم `2xl`.                                                                             |
| `AppDialogs` (`alert` / `confirm`) | ❌ ناقص | غير موحدة؛ يُستدعى `showDialog` محلياً في كل صفحة مع أزرار مختلفة النصوص.                                                                |

---

## 4. هيكل التطبيق وقشرة التنقل Shell (القسم 6)

| العنصر                                    |  الحالة  | الفرق والتقييم التفصيلي                                                                                                                                                                                       |
| ----------------------------------------- | :------: | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| صفحات الدخول والتسجيل المستقلة            | ⚠️ يختلف | تم دمجهما في شاشة واحدة `AuthPage` بتبديل حالة، خلافاً للهيكل المطلوب كصفحتين منفصلتين `/login` و `/register` بخلفية `gray100` بلا حواشٍ خارجية وبطاقة مركزية 448px.                                          |
| الشريط الجانبي والتجاوب (القائمة / الدرج) | ⚠️ يختلف | تم بناء `Drawer` كلاسيكي بـ `AppBar` علوي وإجراءات، بدلاً من: (1) قائمة ثابتة بعرض 256px بتدرج `blue700` إلى `blue900` للشاشات ≥768، و(2) درج وزر عائم عند الموضع الفيزيائي `top: 16, left: 16` للشاشات <768. |
| رأس القائمة وشعار المتجر                  | ⚠️ يختلف | يستخدم أيقونة وصورة خاطئة `iconapp.jpg` مع نصوص ثابتة، بدلاً من شعار `icon.png` بحجم 64×64 وخلفية شفافة 10% واسم المتجر الديناميكي من الإعدادات وتحته «نظام إدارة الأقساط».                                   |
| صندوق الدعم الفني في القائمة              | ❌ ناقص  | غير موجود بالكامل (العنوان، زر الاتصال `07706118992` وزر واتساب).                                                                                                                                             |
| روابط القائمة الثمانية                    | ⚠️ يختلف | الروابط الحالية تعرض 10 عناصر Material، وتحتوي مسارات غير مطلوبة (`Sales`, `Installments`) ولا تطابق الإيموجي وحالات التنشيط والتسميات الثمانية المعتمدة.                                                     |
| تذييل القائمة                             | ❌ ناقص  | رقم الإصدار «v1.0.0» من `AppConfig` غير موجود في أسفل القائمة بتنسيقه المحدد.                                                                                                                                 |
| شريط تنبيه الاشتراك                       | ❌ ناقص  | غير مطبق في الواجهة إطلاقاً؛ لا يظهر صندوق التحذير الأحمر بالأيام المتبقية (0، 1، 2) وأزرار الاتصال والواتساب.                                                                                                |
| حارس المسارات وإعادة التهيئة عند 401      | ⚠️ يختلف | التنقل يدوي عبر `Navigator.push` مكرر، وتفريغ حالة التطبيق والـ Controllers بالكامل عند الخروج يحتاج ضبطاً هيكلياً وفق القسم 6.4.                                                                             |

---

## 5. الشاشات التفصيلية (القسم 7)

### 7.1 تسجيل الدخول `/login` — ⚠️ يختلف

- **الفروقات الدقيقة:**
  - مدمجة مع التسجيل بدلاً من شاشة مستقلة.
  - لا تستخدم الشعار الرسمي `icon.png` بحجم 80×80 داخل بطاقة بخلفية `gray50` وحد `gray200` وظل `sm`.
  - غياب زر التواصل عبر واتساب لتجديد الاشتراك عند استلام كود 403 (`SUBSCRIPTION_STOPPED`) مع إرسال رسالة التجديد الافتراضية.
  - حقول الإدخال لا تطابق مواصفة شاشة الدخول (نصف قطر 4، حد رمادي يتحول للأزرق دون حلقة، LTR للبريد وكلمة المرور).
  - رسائل الأخطاء الافتراضية والنصوص لا تتطابق حرفياً مع المرجع (`Login.vue`).

### 7.2 إنشاء حساب `/register` — ❌ ناقص

- **الفروقات الدقيقة:**
  - غير منفصلة كشاشة مسار مستقل.
  - ينقصها تصميم البطاقة المركزية والحقول الثلاثة بنصوصها وتسميات أزرارها («إنشاء الحساب» / «جاري التسجيل...») ورابط «لديك حساب بالفعل؟ تسجيل الدخول».

### 7.3 لوحة التحكم الرئيسية `/` — ⚠️ يختلف

- **الفروقات الدقيقة:**
  - في الكود الحالي: مدمجة كودياً داخل `main.dart` وتحتوي 4 بطاقات بسيطة لا تمت لتصميم الويب بصلة.
  - غياب بطاقة ربح هذا الشهر المتوقع بتدرج `green500` إلى `emerald600` وزر التقارير.
  - غياب بطاقات الإحصائيات الأربع بتدرجاتها الفيزيائية (`to-br`) وإيموجياتها وأوصافها ونسب الشفافية.
  - غياب بطاقات المبالغ الثلاث (المحصل، المتبقي، المتأخرات) بتفصيل الدولار والمحلي وحدود التنبيه الحمراء.
  - غياب بطاقات أقساط الأسبوع والشهر بتدرجات الكهرمان والأزرق والروابط إلى `/upcoming`.
  - غياب شبكة تنبيهات المخزون المتجاوبة (1/2/3 أعمدة) وحالات نفاد المخزون (`quantity <= 0`).
  - غياب قائمتي «أكثر المواد مبيعاً» و «آخر العمليات» بتنسيق الصفوف والمبالغ والتاريخ المختصر.
  - غياب زر تسجيل الخروج `AppButton.danger` أسفل الصفحة.

### 7.4 المواد `/products` — ⚠️ يختلف

- **الفروقات الدقيقة:**
  - تم تنفيذها كقائمة بطاقات `ListView` مع زر عائم FAB و `CircleAvatar`، بدلاً من جدول بيانات متكامل ببحث مسبق وتمرير أفقي.
  - غياب ترويسة الصفحة بالأزرار: «🏪 المخزن» (secondary) و «➕ إضافة مادة جديدة» (primary).
  - أعمدة الجدول غير مطابقة، وتفتقر لأعمدة سعر الشراء والربح المحسوب (`price - cost`) والعملة كشارات info/warning وتصنيف الألوان.
  - مودال الإضافة/التعديل لا يطابق المرجع (ينقصه صندوق حساب الربح المتوقع ونسبته التلقائية، واختيار العملة، وتنسيق الحقول 448px).
  - تنبيهات الحذف لا تعرض نص الخادم عند ارتباط المادة بعمليات بيع سابقة (خطأ 409).

### 7.5 المخزن `/inventory` — ⚠️ يختلف

- **الفروقات الدقيقة:**
  - نفّذت كبطاقات مادية مع زر تصفية في AppBar بدلاً من شريط الفلاتر بالأزرار الكبسولية مع العدادات: «الكل»، «📊 مُتتبَّع»، «⚠️ منخفض»، «🚫 نفد»، «⏸️ غير مُتتبَّع».
  - غياب شريط التنبيه الإرشادي `InfoBanner` («المخزن اختياري:...»).
  - غياب جدول المخزن المعياري بالألوان الشرطية للصفوف (أحمر لنفاد الكمية، كهرماني للمنخفض) وأزرار الإجراءات الصغيرة الثلاثة (`➕`, `📋`, `⏹️`).
  - مودال الكمية يفتقر لخيارات `set` و `add` وصندوق الكمية الحالية وحد التنبيه.
  - غياب مودال سجل الحركات (Movements) بالكامل (الحركات الـ 50 الأخيرة وتصنيفها بالإيموجي والتدرج اللوني).
  - تأكيد إيقاف التتبع لا يطابق النص المحدد في المرجع.

### 7.6 إضافة بيع `/new-sale` — ⚠️ يختلف

- **الفروقات الدقيقة:**
  - في الكود الحالي تم وضع نافذة منبثقة صغيرة داخل `SalesPage` بدلاً من صفحة كاملة مخصصة كبرى في مسار مستقل `/new-sale`.
  - غياب البحث التلقائي بالـ debounce (300ms) عن المشتري وقائمة الاقتراحات المنسدلة الحية تحت الحقل.
  - غياب جلب حالة المخزون للمادة المختارة وعرض شريط التنبيه الملون المباشر بالكمية المتوفرة ومطابقة التحذيرات.
  - غياب بطاقة ملخص العملية التفاعلية الكاملة (السعر الكلي، المقدمة، المتبقي، قيمة القسط، أول قسط، آخر قسط، والربح المتوقع ونسبته).
  - غياب حوار التحذير عند تجاوز كمية المخزن المتاحة وتأكيد الإكمال بالسالب.
  - إنشاء المشتري الجديد تلقائياً عند عدم وجود معرّف ثم إنشاء البيع والانتقال فوراً لصفحة تفاصيل المشتري غير مطبق بالترتيب السليم.

### 7.7 المشترون `/customers` — ⚠️ يختلف

- **الفروقات الدقيقة:**
  - معروضة كقائمة عمودية بسيطة مع FAB لإضافة مشتري، وتتضمن خيار حذف المشتري (المحظور في القسم 11).
  - المطلوب: شبكة بطاقات متجاوبة (1/2/3 أعمدة)، كل بطاقة تحوي صورة المشتري، رقم الهاتف LTR، بلاطتي إحصاء (إجمالي البيوع والنشط)، وتاريخ الإنشاء «منذ ...»، وشارة المتأخرات في الزاوية اليسرى العليا مع حد أحمر.
  - شريط الفلترة لا يحوي خيارات «الكل»، «نشط»، «عنده متأخرات» بالعدادات.
  - الضغط على البطاقة لا ينقل إلى صفحة تفاصيل المشتري المستقلة `/customers/:id`.

### 7.8 تفاصيل المشتري `/customers/:id` — ❌ ناقص

- **الفروقات الدقيقة:**
  - الصفحة غير موجودة تماماً في المشروع الحالي!
  - ينقصها: بطاقة المشتري الكبرى مع الإحصائيات الأربع، بطاقات المبيعات المستقلة مع شريط التقدم التراكمي وتدرجه اللوني، جدول أقساط البيع وإجراءات الدفع والإلغاء وتحديث الحالة الحية، زر تبديل العملة الفوري بالصرف، زر إرسال تذكير واتساب بالقالب، وملاحظات البيع الصفراء.

### 7.9 طباعة عملية البيع (PDF) — ❌ ناقص

- **الفروقات الدقيقة:**
  - غير مطبقة إطلاقاً، ولا توجد حزم `pdf` أو `printing` في `pubspec.yaml`.
  - ينقصها توليد ملف PDF مقاس A4 بهوامش 10mm لبيانات البيع المحددة فقط بخط Cairo ودعم RTL عبر `Printing.layoutPdf`.

### 7.10 الأقساط القادمة والمتأخرة `/upcoming` — ⚠️ يختلف

- **الفروقات الدقيقة:**
  - نُفذت كبطاقات متباعدة مع `ChoiceChip` بدلاً من جدول بيانات تفصيلي بحدود رقيقة وشريط فلاتر كبسولي (المتأخرة الافتراضي، هذا الأسبوع، هذا الشهر، الكل، المدفوعة).
  - غياب تنسيق الاستحقاق بسطرين (التاريخ وتحته نص `daysUntilText` بألوانه الشرطية: أحمر متأخر، كهرماني ≤ 3 أيام، رمادي غير ذلك).
  - رابط اسم المشتري للانتقال إلى تفاصيله غير مفعل.
  - زر الإجراءات واتساب لا يطبق دالة `formatPhoneForWhatsApp` ببادئة 964.
  - فلتر «المدفوعة» يحتاج تطبيق التصحيح المطلوب (عرض «✅ مدفوع» وإخفاء زر الدفع).

### 7.11 التقارير والأرباح `/reports` — ⚠️ يختلف

- **الفروقات الدقيقة:**
  - تستخدم حزمة الرسوم البيانية `fl_chart` (المحظورة صراحة في القسم 7.11).
  - غياب جلب الأقسام الأربعة بالتوازي (`loadAll` عبر `Promise.all` في الويب).
  - الفلاتر تستخدم قوائم منسدلة عمودية بدلاً من شريط التصفية والتبويبات الأربعة بالأزرار الكبسولية: (ملخص الأرباح، ربح كل مادة، أكثر مبيعاً، الأرباح حسب الفترة).
  - غياب بطاقات الملخص المفصلة لكل عملة ومربعات الإحصاء الأربعة وبطاقات الربح المتوقع والفعلي.
  - غياب تدرج بطاقات المراكز الثلاثة الأولى (🥇 🥈 🥉) في قائمة الأكثر مبيعاً بتدرج الأصفر والبرتقالي وحد `#fde047`.
  - غياب جدول الخط الزمني بأزرار تجميعه (يومي، أسبوعي، شهري، سنوي) وتنسيق الفترات الزمني الدقيق.

### 7.12 الإعدادات `/settings` — ⚠️ يختلف

- **الفروقات الدقيقة:**
  - النموذج يفتقر لتقسيم البطاقات المتباعدة (`max-w-3xl` بفجوة 24) مع عناوينها الزرقاء `xl` وحدودها السفلية.
  - غياب قائمة المتغيرات المتاحة لقالب واتساب مع شرائح الأكواد الرمادية التوضيحية.
  - غياب بطاقة «ℹ️ معلومات النظام» الرمادية بإصدار النظام وشارة نجاح اتصال قاعدة البيانات.
  - التأكد من إرسال المفاتيح الخمسة المسموحة فقط في `PUT` لتجنب خطأ 400.

---

## 6. قواعد الأعمال والتنسيق المشترك (القسم 8)

| القاعدة                |  الحالة  | التقييم والتعديل المطلوب                                                                                            |
| ---------------------- | :------: | ------------------------------------------------------------------------------------------------------------------- |
| `formatAmount`         | ⚠️ يختلف | تعديل التنسيق ليعتمد صيغة `#,##0.##` بالإنجليزية دون رموز ملتصقة، مع إتاحة دمج الرمز حسب الموضع الفيزيائي لكل شاشة. |
| المحللات والمتسامحات   | ✅ مطابق | ملف `parsers.dart` سليم ويغطي المطلوب.                                                                              |
| منسق التواريخ والفترات | ⚠️ يختلف | استكمال `formatDateShort`, `formatDateNumeric`, `formatPeriod`, `daysUntil`, `daysUntilText`.                       |
| قواعد العملة والتبديل  | ⚠️ يختلف | توحيد موضع الرمز (قبل الرقم في مبالغ الرئيسية، وبعد الرقم في باقي الشاشات) والفصل التام بين USD والمحلي.            |
| معاينة تواريخ الأقساط  | ❌ ناقص  | حساب تواريخ أول وآخر قسط في `NewSale` وفق المعادلة الزمنية المحددة.                                                 |
| رابط وتنسيق واتساب     | ⚠️ يختلف | ضبط استبدال المتغيرات وتنظيف الأرقام والتمييز بين استخدام 964 في الأقساط القادمة والأرقام الخام في تفاصيل المشتري.  |
| حسابات ونسب الأرباح    | ⚠️ يختلف | مطابقة حساب الهامش `(profit / revenue) * 100` ونسبة الربح `(profit / cost) * 100` بصيغة `toFixed(1)`.               |

---

## 7. الإضافات والتجهيزات العامة (القسم 12 والاعتماديات)

| العنصر               |  الحالة  | الملاحظات                                                                                                 |
| -------------------- | :------: | --------------------------------------------------------------------------------------------------------- |
| حزم الطباعة وPDF     | ❌ ناقص  | يلزم إضافة `pdf: ^3.11.1` و `printing: ^5.13.3` إلى `pubspec.yaml`.                                       |
| إزالة حزمة الرسوم    | ⚠️ يختلف | إزالة `fl_chart` لعدم الحاجة إليها والتزاماً بالقسم 7.11.                                                 |
| حزمة SVG             | ❌ ناقص  | يلزم إضافة `flutter_svg` لعرض أيقونة واتساب SVG في شاشة الدخول بدقة.                                      |
| ملفات الترجمة `l10n` | ❌ ناقص  | إنشاء `l10n.yaml` و `app_ar.arb` و `app_en.arb` مع التوليد الآلي.                                         |
| سمات المظهر الليلي   | ⚠️ يختلف | إعادة بناء `AppTheme` و `AppColors` ليعكس لوحة Tailwind النهارية حرفياً، مع لوحة ليلية متوازنة المقابلات. |
| التجاوب وقياس الشاشة | ⚠️ يختلف | ضبط `ScreenUtilInit` ليعمل بـ `designSize` 390×844 فقط على الشاشات <768، وقياس حقيقي 1:1 للشاشات الكبرى.  |

---

## 8. خلاصة التدقيق وخطة المراحل التالية

1. **نسبة المطابقة الحالية للواجهات الأصلية:** لا تتجاوز **35%**؛ حيث كان المشروع الحالي عبارة عن مسودة سريعة تعتمد عناصر Material 3 الافتراضية بدلاً من مطابقة عناصر وتصميم الويب (Vue + Tailwind) حرفياً.
2. **المرحلة القادمة المباشرة (المرحلة 1):**
   - تحديث `pubspec.yaml` (إضافة pdf, printing, flutter_svg وإزالة fl_chart).
   - بناء الأساس: `core/theme` (ألوان Tailwind الـ 14، الظلال، الأنماط، التدرجات الفيزيائية)، `core/utils` (الأرقام، التواريخ، الواتساب، العملات)، و `core/config`.
   - بناء ويدجتات `core/widgets` المشتركة بالكامل (13 ويدجت).
   - ضبط `core/l10n` بالملفات العربية والإنجليزية.
3. **المرحلة اللاحقة (المرحلة 2):**
   - بناء الهيكل العام `AdaptiveShell` (القائمة الجانبية المتجاوبة، تنبيه الاشتراك، الدرج، والتنقل).
   - تصحيح وبناء الشاشات الـ 11 شاشة تلو الأخرى بالترتيب، واختبار كل شاشة مع المرجع.

---

## 9. حالة الإنجاز النهائي والتحقق (تم الاكتمال بنجاح)

- [x] **المرحلة 0 و 1:** تدقيق شامل، بناء كامل لنظام الرموز والألوان والظلال والويدجتات المشتركة ودوال المساعدة والتحويلات.
- [x] **المرحلة 2:** اكتمال بناء وربط الشاشات الـ 11 كاملة بمطابقة دقيقة لـ Vue 3 + Tailwind (الهيكل المتجاوب، المصادقة، لوحة التحكم، المنتجات، المخزون، المشترين، تفاصيل المشتري، بيع جديد، الأقساط القادمة، التقارير الصرفة بدون رسوم بيانية، الإعدادات).
- [x] **خدمة تصدير وطباعة PDF:** مطابقة ورقة A4 RTL بالخط المضمن Cairo و4 مربعات إحصائية وجدول الأقساط وتذييل التواقيع.
- [x] **فحص الكود (Analyzer):** نتيجة `flutter analyze` هي **0 أخطاء و 0 تحذيرات و 0 تنبيهات**.
- [x] **حزمة الاختبارات (Unit Tests):** اجتياز **50 اختباراً بنجاح 100%** يغطي المنسقات والمحللات وحسابات الفوائد والأقساط والأرباح واستجابات الـ API.

