name: aksat
description: "نظام إدارة الأقساط والبيع بالتقسيط - تطبيق Flutter كلي."
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ^3.13.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  provider: ^6.1.5
  dio: ^5.8.0
  flutter_secure_storage: ^11.2.0
  shared_preferences: ^2.3.5
  flutter_screenutil: ^5.9.0
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.3
  url_launcher: ^6.3.1
  pdf: ^3.11.1
  printing: ^5.13.3
  flutter_svg: ^2.0.17

flutter:
  uses-material-design: true
  generate: true
  assets:
    - assets/fonts/
    - assets/images/
    - assets/icons/

  fonts:
    - family: Cairo
      fonts:
        - asset: assets/fonts/cairo-variable.ttf

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
